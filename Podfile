# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'RSSFeedDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RSSFeedDemo
  pod 'FeedKit', '~> 5.0'
  pod 'Alamofire', '~> 4.3'
  pod 'Kingfisher', '~> 3.0’

  target 'RSSFeedDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RSSFeedDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
